<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MF.Presentation.Views.FormaPagamentoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MF.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loading="Page_Loading"
    mc:Ignorable="d">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="38"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Foreground="{StaticResource Cinza3}"
                   FontSize="12"
                   Padding="8,8,0,0"
                   FontWeight="Bold"
                   Text="Forma de pagamento" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top"/>

        <Frame Grid.Row="1" Background="{StaticResource Cinza9}">
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" Orientation="Horizontal" Spacing="4">
                <Button x:Name="btnDeletar" Height="28" Width="28" Padding="0" Click="btnDeletar_Click" IsEnabled="{Binding HabilitarBotaoEditarExcluir}">
                    <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                </Button>
                <Button x:Name="btnEditar" Height="28" Width="28" Padding="0" Margin="0" Click="btnEditar_Click" IsEnabled="{Binding HabilitarBotaoEditarExcluir}">
                    <FontIcon Glyph="&#xE70F;" FontSize="14"/>
                </Button>
                <Button x:Name="btnAdicionar" Height="28" Width="28" Padding="0" Margin="0" Style="{StaticResource AccentButtonStyle}" Click="btnAdicionar_Click" >
                    <FontIcon Glyph="&#xE710;" FontSize="14"/>
                </Button>
            </StackPanel>
        </Frame>
        
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Cabeçalho -->
            <Grid  Grid.Row="0" Style="{StaticResource GridCabecalho}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="48"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="48"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="1" Style="{StaticResource BorderCelulaComSeparador}">
                    <TextBlock Text="" Style="{StaticResource TextBlockCabecalho}"/>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource BorderCelulaComSeparador}">
                    <TextBlock Text="Ativo" Style="{StaticResource TextBlockCabecalho}"/>
                </Border>

                <Border Grid.Column="3" Style="{StaticResource BorderCelulaComSeparador}">
                    <TextBlock Text="Forma de Pagamento" Style="{StaticResource TextBlockCabecalho}"/>
                </Border>

                <Border Grid.Column="4" Style="{StaticResource BorderCelulaComSeparador}">
                    <TextBlock Text="Tipo de Transação" Style="{StaticResource TextBlockCabecalho}"/>
                </Border>

                <Border Grid.Column="5" Style="{StaticResource BorderCelulaComSeparador}">
                    <TextBlock Text="" Style="{StaticResource TextBlockCabecalho}"/>
                </Border>
            </Grid>

            <!-- Linhas -->
            <ListView x:Name="lstPrincipal" Grid.Row="1" Style="{StaticResource ListViewDataGrid}" SelectionChanged="lstPrincipal_SelectionChanged">

                <ListView.ItemTemplate>
                    <DataTemplate >
                        <Grid Style="{StaticResource GridLinha}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="48"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="48"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource BorderCelulaComSeparador}">
                                <TextBlock Text="{Binding PK_FormaPagamento}" Style="{StaticResource TextBlockDadoCentralizado}"/>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource BorderCelulaComSeparador}">
                                <TextBlock Text="{Binding Ativo}" Style="{StaticResource TextBlockDadoLinha}" Foreground="{Binding AtivoCor}"/>
                            </Border>

                            <Border Grid.Column="3" Style="{StaticResource BorderCelulaComSeparador}">
                                <TextBlock Text="{Binding FormaPagamento}" Style="{StaticResource TextBlockDadoCentralizado}"/>
                            </Border>

                            <Border Grid.Column="4" Style="{StaticResource BorderCelulaComSeparador}">
                                <TextBlock Text="{Binding TipoTransacao}" Style="{StaticResource TextBlockDadoCentralizado}"/>
                            </Border>

                            <Border Grid.Column="5" Style="{StaticResource BorderCelulaComSeparador}">
                                <FontIcon Glyph="{Binding TipoTransacaoIcone}" Foreground="{Binding TipoTransacaoCor}" Style="{StaticResource FontIconStatusFormaPagamento}"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
